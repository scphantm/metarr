{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // .tool_response.filePath // empty' | { read -r f; case \"$f\" in *.md|*.markdown|*.mdx) cd \"${CLAUDE_PROJECT_DIR:-.}\" && make fmt ;; esac; } 2>/dev/null || true",
            "timeout": 120,
            "statusMessage": "Formatting markdown (make fmt)..."
          }
        ]
      }
    ]
  }
}
