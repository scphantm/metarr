basePath: /
definitions:
  handlers.HeartbeatResponse:
    properties:
      correlation_id:
        type: string
      time:
        type: string
      version:
        type: string
    type: object
info:
  contact: {}
  description: Event-driven Go API backed by MongoDB and Redis. Every request carries
    a correlation ID (X-Correlation-ID), generated by the server if the caller doesn't
    supply one.
  title: Metarr API
  version: "1.0"
paths:
  /api/heartbeat:
    get:
      description: Publishes a request on a Redis Pub/Sub channel and blocks until
        the in-process heartbeat listener replies with the current time and the request's
        correlation ID. Confirms the server's Redis request/reply path; it does not
        reach any agent.
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/handlers.HeartbeatResponse'
        "429":
          description: too many requests
          schema:
            type: string
        "504":
          description: heartbeat timed out
          schema:
            type: string
      summary: Redis round-trip health check
      tags:
      - Heartbeat
securityDefinitions:
  ApiKeyHeaderAuth:
    description: API key via the X-Api-Key header.
    in: header
    name: X-Api-Key
    type: apiKey
  ApiKeyQueryAuth:
    description: API key via the apikey query parameter. Either this or ApiKeyHeaderAuth
      satisfies auth on protected routes.
    in: query
    name: apikey
    type: apiKey
swagger: "2.0"
