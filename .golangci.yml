version: "2"

# Deliberately no linters.enable/disable list here: this project runs
# golangci-lint's own default set of linters, not a hand-picked one.

issues:
  exclude-dirs:
    # Generated by `swag init` (see generate.go) — nobody hand-edits this.
    - docs
